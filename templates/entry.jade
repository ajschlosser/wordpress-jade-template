article(id!="<?php the_ID(); ?>", class!="<?php post_class(); ?>")
	header
		- if ( is_singular() ) { echo '<h1 class="entry-title">'; } else { echo '<h2 class="entry-title">'; }
		a(href!="<?php the_permalink(); ?>", title!="<?php the_title_attribute(); ?>", rel="bookmark")
			- the_title();
		- if ( is_singular() ) { echo '</h1>'; } else { echo '</h2>'; edit_post_link();
	- get_template_part( 'entry', ( is_archive() || is_search() ? 'summary' : 'content' ) );
	- if ( !is_search() ) get_template_part( 'entry-footer' );